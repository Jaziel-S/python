# 我的奇奇怪怪的Python文件存档
## Github创建文件夹的方法：

打开你的仓库 → 点击 Add file → Create new file。

在文件名输入框里，输入类似：

···
docs/readme.md
···
这里的 docs 就是新文件夹的名字。

GitHub 会自动在仓库里创建 docs 文件夹，并在里面放一个 readme.md 文件。

编辑文件内容 → 点击 Commit new file，提交后文件夹就存在了。

---
